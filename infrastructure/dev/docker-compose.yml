services:
  mongodb:
    image: mongo:7
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_DB_ROOT_USERNAME: admin
      MONGO_DB_ROOT_PASSWORD: password
    volumes:
      - ./data/mongo:/data/db